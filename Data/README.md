`Texas_more_covariates_auto.R` calls ALARM dataset, `BlockAssign_ST48_TX_VTD` and tidycensus package in order to construct a comprehensive dataset of electoral results and socio-economic characteristics for each electoral precinct in Texas.

In the end, it produces the dataset named `texas_with_covariates.rds`.
