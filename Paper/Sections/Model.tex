Here we introduce the model in which Y is a binary variable and therefore we can convert it into a one-dimensional variable that takes 1 or 0 for each individual, and shares in geographies.

Then X is the previously presented categorical variable and Z is a set of geographical covariates.

We assume that the relationship is as presented in the previous section.

%Probabilmente la cosa migliore è assumere CAR-IND e citare McCartan nel dimostrare che CAR-IND implica CAR


%Finally, we briefly present how seine works under the hood, as far as I can understand it. (anche no)